# Serverless Manual Sync

Mexx Tracker is serverless in version 1: it runs as an offline-first PWA, stores data locally on the device, and moves data between devices only through explicit JSON export and import. This avoids accounts, cloud infrastructure, and automatic synchronization complexity, while accepting that backups and day transfers are manual user actions.
